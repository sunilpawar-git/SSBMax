<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Phase 5 canary validation harness (debug builds only). Supersedes
         the Phase 0 KmpSpikeActivity, deleted this phase. -->
    <application>
        <activity
            android:name="com.ssbmax.spike.CanaryActivity"
            android:exported="true"
            android:theme="@style/Theme.SSBMax" />
    </application>

</manifest>
